import {RECEIVE__COURSEINFO } from '../mutation-types'
import { reCourse } from '../../api'
const state = {
  
}
const mutations = {
  
}
const actions = {
  
}
const getters = {
}
export default {
  state,
  mutations,
  actions,
  getters
}